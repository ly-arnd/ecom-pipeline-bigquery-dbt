# ecom-pipeline-bigquery-dbt

Terraform basic commands to run the project:
```bash
cd terraform
set -a && source ../.env && set +a
terraform plan

terraform init
terraform plan
terraform apply
terraform destroy
terraform output
```

For local development
```angular2html
gcloud auth login
gcloud config list
```